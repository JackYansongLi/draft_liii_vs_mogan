% \documentclass{article}
% \usepackage{amsmath}

% \begin{document}

% The equation is:
% \begin{equation}
% x=\frac{-b\pm \sqrt{b^{2}-4ac}}{2a}
% \end{equation}

% \begin{align}  % 使用 align 环境支持多行公式和 & 对齐
%    \lambda_{q_*,q_i} &=  \frac{ W_{q_*,q_i} }{ \sum_{q_j\in \text{Neb}(q_*)} W_{q_*,q_j}}
%    I_{\text{loc}} (q_*) & = \sum_{q_i\in \text{Neb}(q_*)} \lambda_{q_*,q_i} I_{q_i}
% \label{eq:4.4}  % 标签放在 align 环境内（自动关联到第二行）
% \end{align}

% \end{document}

\documentclass{ctexart}
% \usepackage{amsmath}
\begin{document}
$E = mc^2 \text{（能量公式）}$
\end{document}
